development:
  clients:
    default:
      database: myapp_development
      hosts:
        - <%= ENV['MONGO_HOST'] ||= "localhost:27017" %>
      options:
  options:
    
test:
  clients:
    default:
      database: myapp_test
      hosts:
        - <%= ENV['MONGO_HOST'] ||= "localhost:27017" %>
      options:
        read:
          mode: :primary
        max_pool_size: 1

production:
  clients:
    default:
      uri: mongodb+srv://dbuser1:<password>@capstone-staging-7sppw.mongodb.net/test?retryWrites=true&w=majority
      options:
        connect_timeout: 15